{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ name|upper }}! ✅</h1>

    {#
    <a href="{{ url('product') }}">Go to my product page</a>
    #}

    {% if age > 18 %}
        <p>You are an adult, {{ name }}</p>
    {% else %}
        <p>You are a child</p>
    {% endif %}

    <ul>
        {% for key, animal in animals %}
            <li>{{ animal }} / {{ key }}</li>
        {% else %}
            <li>No animals found</li>
        {% endfor %}
    </ul>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/grubolsch/Desktop/code/symfony/tutorial/src/Controller/HomepageController.php'|file_link(0) }}">src/Controller/HomepageController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/grubolsch/Desktop/code/symfony/tutorial/templates/homepage/index.html.twig'|file_link(0) }}">templates/homepage/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}